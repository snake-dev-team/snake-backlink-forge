-- Queries for the api_keys table.
-- Phase 2+ will add real queries here (key generation, revocation, lookup by hash).
-- Placeholder kept so sqlc can parse this file without errors.

-- name: PlaceholderKeysSelect :one
SELECT 1 AS dummy;
