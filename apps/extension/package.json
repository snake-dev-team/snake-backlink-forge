{
  "name": "@sbf/extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build",
    "dev": "vite",
    "package:check": "npm run build && npm run validate:manifest",
    "typecheck": "tsc --noEmit && svelte-check --tsconfig tsconfig.json",
    "validate:manifest": "node scripts/validate-manifest.mjs"
  },
  "dependencies": {
    "svelte": "^5"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "2.4.0",
    "@sveltejs/vite-plugin-svelte": "^3",
    "@types/chrome": "latest",
    "svelte-check": "latest",
    "typescript": "6.0.3",
    "vite": "^5"
  }
}
