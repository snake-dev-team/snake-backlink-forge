# Snake Backlink Forge — local dev compose vars
# Copy to .env before first run: cp .env.example .env
# LOCAL DEV ONLY — never commit .env to git.

POSTGRES_USER=sbf
POSTGRES_PASSWORD=sbf
POSTGRES_DB=sbf_dev
POSTGRES_PORT=5432
REDIS_PORT=6379
